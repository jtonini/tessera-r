% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tessera.R
\name{tessera}
\alias{tessera}
\title{Run the Full TESSERA Pipeline}
\usage{
tessera(
  data,
  time_col = NULL,
  entity_col = NULL,
  preset = NULL,
  measures = NULL,
  n_bins = NULL,
  bin_method = NULL,
  network_method = NULL,
  k = NULL,
  community_algorithm = NULL,
  resolution = NULL,
  divergence_method = NULL,
  divergence_reference = NULL,
  scale = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data.frame of entity features with a temporal column.}

\item{time_col}{Character. Name of the temporal column.}

\item{entity_col}{Character or \code{NULL}. Name of the entity ID column.}

\item{preset}{Character or \code{NULL}. Named parameter preset:
\code{"ecology"} — optimized for ecological observational data (quantile bins,
Bray-Curtis + Simpson measures, Leiden communities);
\code{"manufacturing"} — optimized for sensor/quality data (fixed bins,
Mahalanobis + Euclidean, spectral communities);
\code{"hpc"} — optimized for HPC job data (quantile bins, cosine + Pearson,
Louvain communities);
\code{NULL} — uses defaults.}

\item{measures}{Character vector. Similarity measures (see \code{\link[=tessera_similarity]{tessera_similarity()}}).}

\item{n_bins}{Integer. Number of temporal bins.}

\item{bin_method}{Character. \code{"quantile"} or \code{"fixed"}.}

\item{network_method}{Character. Network construction method.}

\item{k}{Integer. kNN parameter.}

\item{community_algorithm}{Character. Community detection algorithm.}

\item{resolution}{Numeric. Resolution parameter for community detection.}

\item{divergence_method}{Character. Divergence measure.}

\item{divergence_reference}{Character. Divergence reference strategy.}

\item{scale}{Logical. Standardize features. Default \code{TRUE}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A \code{tessera_result} object containing all intermediate results:
\verb{$characterize}, \verb{$similarity}, \verb{$network}, \verb{$communities}, \verb{$divergence}.
}
\description{
Convenience wrapper that runs Steps 1–3 of the TESSERA framework in a single
call: characterize entities, compute similarity, build networks, detect
communities, and quantify regime divergence. For fine-grained control, use
the individual pipeline functions.
}
\examples{
set.seed(42)
df <- data.frame(
  year = sample(2000:2020, 500, replace = TRUE),
  temp = rnorm(500, 25, 3),
  depth = runif(500, 0, 50),
  salinity = rnorm(500, 35, 1)
)
result <- tessera(df, time_col = "year")
result
plot(result)

}
