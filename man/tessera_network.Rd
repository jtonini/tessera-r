% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{tessera_network}
\alias{tessera_network}
\title{Construct Similarity Networks}
\usage{
tessera_network(
  x,
  method = c("knn", "threshold", "weighted"),
  k = 10L,
  threshold = 0.5,
  measure = NULL
)
}
\arguments{
\item{x}{A \code{tessera_similarity} object.}

\item{method}{Character. Network construction method:
\code{"knn"} (default) — connect each entity to its k nearest neighbors;
\code{"threshold"} — connect entities with similarity above a threshold;
\code{"weighted"} — fully connected weighted graph (only for small datasets).}

\item{k}{Integer. Number of neighbors for kNN method. Default 10. If the
similarity object was already sparsified with a larger k, this further
prunes edges.}

\item{threshold}{Numeric in [0, 1]. Similarity threshold for the threshold
method. Default 0.5.}

\item{measure}{Character. Which similarity measure to use for network
construction. Default uses the first available measure.}
}
\value{
A \code{tessera_network} object containing:
\describe{
\item{graph}{An \link[igraph:aaa-igraph-package]{igraph::igraph} graph object with similarity as edge weights}
\item{method}{Construction method used}
\item{measure}{Similarity measure used}
\item{params}{List of parameters}
}
}
\description{
Step 2b of the TESSERA pipeline. Converts similarity matrices into graph
objects using kNN, threshold, or weighted methods. The resulting network
encodes the topological structure that community detection will partition.
}
\examples{
set.seed(42)
mat <- matrix(rnorm(200), nrow = 50, ncol = 4)
sim <- tessera_similarity(mat, measures = "cosine", k = 10)
net <- tessera_network(sim, method = "knn", k = 8)
summary(net)

}
