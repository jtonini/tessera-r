% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/characterize.R
\name{tessera_characterize}
\alias{tessera_characterize}
\title{Characterize Entities via Temporal Binning}
\usage{
tessera_characterize(
  data,
  time_col = NULL,
  entity_col = NULL,
  n_bins = 4L,
  bin_method = c("quantile", "fixed"),
  feature_cols = NULL,
  min_entities = 30L,
  scale = TRUE
)
}
\arguments{
\item{data}{A data.frame or matrix of entity features. Rows are entities,
columns are features.}

\item{time_col}{Character string naming the column that contains temporal
information (e.g., year, timestamp). If \code{NULL}, all entities are treated
as a single bin.}

\item{entity_col}{Character string naming the column that identifies
individual entities. If \code{NULL}, row indices are used.}

\item{n_bins}{Integer. Number of temporal bins. Default is 4.}

\item{bin_method}{Character. Either \code{"quantile"} (default, recommended) or
\code{"fixed"} for equal-width bins. Quantile-based binning ensures balanced
entity counts across bins, which is critical for datasets with uneven
temporal distributions.}

\item{feature_cols}{Character vector of column names to use as features.
If \code{NULL}, all numeric columns except \code{time_col} and \code{entity_col} are used.}

\item{min_entities}{Integer. Minimum number of entities required per bin.
Bins with fewer entities are merged with adjacent bins. Default is 30.}

\item{scale}{Logical. Whether to standardize features to zero mean and unit
variance within each bin. Default is \code{TRUE}.}
}
\value{
A \code{tessera_characterize} object (S3) containing:
\describe{
\item{data}{The processed data.frame with bin assignments}
\item{bins}{A data.frame describing bin boundaries and entity counts}
\item{features}{Character vector of feature column names used}
\item{time_col}{Name of the time column}
\item{entity_col}{Name of the entity column}
\item{params}{List of parameters used}
}
}
\description{
Step 1 of the TESSERA pipeline. Assigns entities to temporal bins using
quantile-based or fixed-width boundaries, validates features, and prepares
the data structure for downstream similarity computation.
}
\examples{
# Synthetic example
set.seed(42)
df <- data.frame(
  year = sample(2000:2020, 500, replace = TRUE),
  temp = rnorm(500, 25, 3),
  depth = runif(500, 0, 50),
  salinity = rnorm(500, 35, 1)
)
char <- tessera_characterize(df, time_col = "year", n_bins = 4)
summary(char)

}
